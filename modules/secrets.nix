{ config, lib, ... }:

{
  # For now, we're disabling direct imports until the flake structure is fixed
  imports = [];

  # Placeholder for secrets configuration
  # This module is temporarily simplified to avoid bootstrap issues
  # TODO: Re-enable secret management after flake structure is fixed
} 